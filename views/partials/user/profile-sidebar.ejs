<div class="hidden md:flex flex-col p-6 sticky top-24 h-[calc(100vh-6rem)] w-64 border-r border-white/5">
    <nav class="space-y-2">
        <a href="/user/profile"
            class="<%= locals.currentPage === 'profile' ? 'bg-brand-accent text-brand-bg1' : 'text-brand-light hover:bg-brand-bg2' %> block px-4 py-3 font-medium rounded-lg transition-colors">Personal
            Info</a>
        <a href="/user/orders"
            class="<%= locals.currentPage === 'orders' ? 'bg-brand-accent text-brand-bg1' : 'text-brand-light hover:bg-brand-bg2' %> block px-4 py-3 font-medium rounded-lg transition-colors">My
            Orders</a>
        <a href="/user/address"
            class="<%= locals.currentPage === 'address' ? 'bg-brand-accent text-brand-bg1' : 'text-brand-light hover:bg-brand-bg2' %> block px-4 py-3 font-medium rounded-lg transition-colors">Address</a>
        <a href="/user/wallet"
            class="<%= locals.currentPage === 'wallet' ? 'bg-brand-accent text-brand-bg1' : 'text-brand-light hover:bg-brand-bg2' %> block px-4 py-3 font-medium rounded-lg transition-colors">Wallet</a>
        <a href="/user/settings"
            class="<%= locals.currentPage === 'settings' ? 'bg-brand-accent text-brand-bg1' : 'text-brand-light hover:bg-brand-bg2' %> block px-4 py-3 font-medium rounded-lg transition-colors">Settings</a>
    </nav>

    <div class="mt-auto pt-10">
        <button onclick="handleLogout()"
            class="flex items-center px-4 py-3 text-red-600 hover:bg-red-50 font-medium rounded-lg transition-all w-full">
            <span class="mr-3">🚪</span> Logout
        </button>
    </div>
</div>

<div class="md:hidden fixed bottom-0 left-0 right-0 bg-brand-bg1 border-t border-white/5 px-2 py-3 z-50">
    <div class="flex justify-around items-center">
        <a href="/user/profile"
            class="flex flex-col items-center <%= locals.currentPage === 'profile' ? 'text-brand-light' : 'text-brand-muted' %>">
            <span class="text-xs mt-1">Profile</span>
        </a>
        <a href="/user/orders"
            class="flex flex-col items-center <%= locals.currentPage === 'orders' ? 'text-brand-light' : 'text-brand-muted' %>">
            <span class="text-xs mt-1">Orders</span>
        </a>
        <a href="/user/settings"
            class="flex flex-col items-center <%= locals.currentPage === 'settings' ? 'text-brand-light' : 'text-brand-muted' %>">
            <span class="text-xs mt-1">Settings</span>
        </a>
        <button onclick="handleLogout()" class="flex flex-col items-center text-red-500">
            <span class="text-xs mt-1">Logout</span>
        </button>
    </div>
</div>